# Configuración para Railway con @sparticuz/chromium
[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install']

[start]
cmd = 'node server.js'

[variables]
NODE_ENV = 'production'
